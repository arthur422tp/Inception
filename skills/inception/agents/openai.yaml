interface:
  display_name: "Inception"
  short_description: "Human-gated review of content defaults"
  default_prompt: "Use $inception to draft or audit this text against my intent, use an independent reviewer when available, show me the Decision Ledger, and wait for my decisions before revising."
