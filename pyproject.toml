[project]
name = "inception"
version = "0.2.0"
description = "Intent-preserving content audit skill with fiction, presentation-text, document-text, and social-copy adapters"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
